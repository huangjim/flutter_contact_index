## 0.1.0

- **Breaking:** Rename package to `flutter_contact_index` for public release.
- **Breaking:** Rename Dart API to `FlutterContactIndex`.
- **Breaking:** Rename Android package to `com.hzm.flutter_contact_index`.
- **Breaking:** Rename iOS pod and plugin symbols to `flutter_contact_index` / `FlutterContactIndex*`.
- Add open-source metadata and GitHub links.
- Document iOS index behavior differences and `CFBundleLocalizations` requirement in README.
